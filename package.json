{
  "name": "order-in-space",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && node scripts/clean-app-dist.mjs && vite build && node scripts/prepare-github-pages.mjs",
    "build:packages": "npm run build --workspace @order-in-space/geometry && npm run build --workspace @order-in-space/render",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "dev": "npm run build:packages && vite",
    "preview": "vite preview",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run check:boundaries && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/three": "0.185.4",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vitest": "4.1.10"
  },
  "dependencies": {
    "@order-in-space/geometry": "*",
    "@order-in-space/render": "*",
    "three": "0.185.1"
  },
  "engines": {
    "node": ">=20.19"
  }
}
