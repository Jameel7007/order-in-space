# Render package

Renderer adapters and visual primitives. This package consumes `@order-in-space/geometry` and Three.js, but knows nothing about scrolling or narrative order. Its first implementation uses instanced cylinders for spatially stable edges, restrained triangulated faces, vertex markers, and instanced construction spheres.
